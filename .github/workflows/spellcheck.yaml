name: Spellcheck

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  spellcheck:
    name: spellcheck tex files
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v4
      - uses: crate-ci/typos@v1.50.0
        with:
          files: "**/*.tex"

